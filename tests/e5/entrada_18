funcao_complexa => int
{
    int x <= 5;
    int y <= 10;
    int result <= 0;

    while (x > 0) {
        x = x - 1;
        int z <= 3;
        while (z > 0) {
            z = z - 1;
            if (y > 0) {
                int temp <= 2;
                if (temp > 10) {
                    result = result + temp;
                } else {
                    int modifier <= 2;
                    while (modifier > 0) {
                        modifier = modifier - 1;
                        result = result + modifier;
                    };
                };
            } else {
                int fallback <= 100;
                result = result + fallback;
            };
        };
    };
    return result;
}
